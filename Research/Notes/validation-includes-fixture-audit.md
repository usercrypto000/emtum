# Validation Includes Fixture Audit
*May 2026*

`npm run validate` now runs the SAP fixture audit after proof generation and verifier-call export. The TypeScript validation path therefore confirms Poseidon2 alignment, Merkle inclusion, negative rejection, verifier-call export, and fixture metadata consistency in one command.
