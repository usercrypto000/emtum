# SDK Read Interfaces
*May 2026*

The repo now includes read interfaces for the authorization status view and task lifecycle view. These interfaces are a small SDK boundary for external integrations that want to consume Emtun state without binding directly to every concrete implementation. They do not introduce write authority or alter settlement semantics.
