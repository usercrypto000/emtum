# Reproducibility Screenshot Path
*May 2026*

The current screenshot path for primitive review is `forge test --match-path test/PrimitiveBoundarySmoke.t.sol -vvv` from `contracts`, paired with `npm run sap-fixture-audit` from `scripts`. The first command shows the onchain-facing public claim. The second command shows the proof fixture metadata that feeds the verifier surface.
