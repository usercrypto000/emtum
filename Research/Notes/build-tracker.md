# Emtun Build Tracker
*May 2026*

This tracker records the public simulation builds for Emtun. Each build must preserve the main boundary: Emtun proves scoped authorization against a committed policy root, not execution correctness.

| Build | Commit | Status | What Changed | What It Proves | Screenshot Command |
| --- | --- | --- | --- | --- | --- |
| 01. Scaffold and SAP circuit baseline | `ca95754` | Shipped | Project scaffold, Noir circuit package, Foundry surface, TypeScript proof tooling, Poseidon2 leaf alignment, Merkle inclusion harness | TypeScript and Noir agree on the Poseidon2 leaf hash, the inclusion proof verifies locally, and negative Merkle cases fail | `cd scripts; npm run validate` |
| 02. Repo sync with validated SAP state | `c7956ea` | Shipped | Public repo cleanup and validated SAP state documentation | The repo can be cloned and rerun around the core SAP proof path | `cd scripts; npm run validate` |
| 03. Verifier gas baseline | `5254c1c` | Shipped | EVM verifier fixture and gas measurement | A single Barretenberg verifier call is measurable and expensive enough to justify future aggregation work | `cd contracts; forge test --match-path test/VerifierGas.t.sol -vvv` |
| 04. Verifier adapter boundary | `edf2011` | Shipped | Adapter around the generated Honk verifier | Contracts call the stable SAP interface, not generated verifier internals | `cd contracts; forge test --match-path test/EmtunVerifierAdapter.t.sol -vvv` |
| 05. Policy root chain | `63eabb6` | Shipped | Chain-head policy root contract with historical records and no version window | Only the current root is valid for authorization; older roots remain historical but not accepted | `cd contracts; forge test --match-path test/PolicyRootChain.t.sol -vvv` |
| 06. Authorization reader | `ad94d05` | Shipped | Current-root lookup plus proof verification reader | Stale proofs fail after policy root rotation | `cd contracts; forge test --match-path test/EmtunAuthorizationReader.t.sol -vvv` |
| 07. Agent registry boundary | `e17ffb7` | Shipped | Agent registration and ownership boundary | Agent identity exists separately from policy evolution | `cd contracts; forge test --match-path test/AgentRegistry.t.sol -vvv` |
| 08. EAS attestation boundary | `6719a69` | Shipped | Local EAS mock and identity attestation boundary | Identity attestation points to the registry and chain-head mechanism, not one root value | `cd contracts; forge test --match-path test/EmtunEASAttestationBoundary.t.sol -vvv` |
| 09. Task authorization gate | `8d2cd7d` | Shipped | Gate combining registration, active attestation, and SAP verification | A task-level authorization check passes only when identity and current-root authorization both hold | `cd contracts; forge test --match-path test/TaskAuthorizationGate.t.sol -vvv` |
| 10. Owner transfer attestation invalidation | `0208f42` | Shipped | EAS boundary binds active attestation to current registry owner | Owner transfer invalidates usable identity attestation until the new owner re-attests | `cd contracts; forge test --match-path test/EmtunEASAttestationBoundary.t.sol -vvv` |
| 11. Full authorization lifecycle smoke | `e26de0f` | Shipped | Single smoke test logging the full lifecycle | Register, attest, authorize, reject stale root, invalidate on transfer, and re-attest all work in one auditable path | `cd contracts; forge test --match-path test/EmtunSimulationSmoke.t.sol -vvv` |
| 12. Task intent market | `6629094` | Shipped | Requester task intents and owner-gated agent claims through SAP authorization | A task can be assigned only when the current agent owner submits the claim and SAP authorization verifies | `cd contracts; forge test --match-path test/EmtunSimulationSmoke.t.sol -vvv` |
| 13. Build tracker | `5b39f7a` | Shipped | Public tracker for shipped builds, proof claims, and screenshot commands | The project now has a canonical development ledger before escrow or execution work begins | `cd contracts; forge test --match-path test/EmtunSimulationSmoke.t.sol -vvv` |
| 14. Task intent stateful fuzz | `4794fa9` | Shipped | Bounded stateful fuzz model for task opening, claiming, and cancellation | Task IDs stay monotonic, assigned and cancelled tasks stay terminal, and onchain task state matches the model across fuzzed operation sequences | `cd contracts; forge test --match-path test/TaskIntentMarketStatefulFuzz.t.sol -vvv` |
| 15. Task funding escrow | `e9e3940` | Shipped | ETH funding and cancellation refund for task intents | Requesters can fund open task intents and recover funds after cancellation, while agent payout remains out of scope | `cd contracts; forge test --match-path test/TaskFundingEscrow.t.sol -vvv` |
| 16. Task result registry | `058120f` | Shipped | Assigned agent result hash commitment | The assigned agent owner can commit an output hash for accountability without proving execution correctness or triggering settlement | `cd contracts; forge test --match-path test/TaskResultRegistry.t.sol -vvv` |
| 17. Task acceptance registry | `0cce68f` | Shipped | Requester acceptance of committed result hashes | The requester can accept the exact committed result hash, creating a future settlement predicate without proving execution correctness | `cd contracts; forge test --match-path test/TaskAcceptanceRegistry.t.sol -vvv` |
| 18. Task settlement release | `736b398` | Shipped | Accepted task escrow release to the current assigned agent owner | Requester acceptance can trigger payment without claiming execution correctness | `cd contracts; forge test --match-path test/TaskFundingEscrow.t.sol -vvv` |
| 19. Task acceptance stateful fuzz | `736b398` | Shipped | Bounded fuzz model for acceptance state and rejected acceptance attempts | Acceptance remains requester-only, exact-hash, and terminal across operation sequences | `cd contracts; forge test --match-path test/TaskAcceptanceRegistryStatefulFuzz.t.sol -vvv` |
| 20. Task result stateful fuzz | `736b398` | Shipped | Bounded fuzz model for result commits, owner transfer, and rejected commits | Only the current assigned agent owner can commit one nonzero result hash | `cd contracts; forge test --match-path test/TaskResultRegistryStatefulFuzz.t.sol -vvv` |
| 21. Task funding stateful fuzz | `736b398` | Shipped | Bounded fuzz model for funding, cancellation, and refunds | Escrow balances, payer records, and cancellation refunds stay aligned with modeled task state | `cd contracts; forge test --match-path test/TaskFundingEscrowStatefulFuzz.t.sol -vvv` |
| 22. Task lifecycle view | `736b398` | Shipped | Read-only task lifecycle snapshot across market, escrow, result, and acceptance state | Clients can audit task state without giving the view contract any write authority | `cd contracts; forge test --match-path test/TaskLifecycleView.t.sol -vvv` |
| 23. Task settlement stateful fuzz | `387dd8e` | Shipped | Bounded fuzz model across open, fund, claim, commit, accept, release, cancel, and refund | Released escrows always correspond to accepted result hashes and modeled balances | `cd contracts; forge test --match-path test/TaskSettlementStatefulFuzz.t.sol -vvv` |
| 24. Payout failure coverage | `387dd8e` | Shipped | Regression test for rejecting agent-owner payout recipients | Failed payouts revert atomically and leave escrow funded | `cd contracts; forge test --match-path test/TaskFundingEscrow.t.sol -vvv` |
| 25. Lifecycle audit fields | `387dd8e` | Shipped | Read-only lifecycle view now exposes payer, result agent, and timestamps | Clients can audit lifecycle state from one snapshot without new write authority | `cd contracts; forge test --match-path test/TaskLifecycleView.t.sol -vvv` |
| 26. Deployment wiring test | `387dd8e` | Shipped | Foundry test for deployment script code presence and dependency wiring | Local simulation deployment cannot silently drift across constructor dependencies | `cd contracts; forge test --match-path test/DeploySimulation.t.sol -vvv` |
| 27. Smoke lifecycle log | `387dd8e` | Shipped | Full smoke test logs released lifecycle status after settlement | Public demo output shows settlement state after acceptance-triggered release | `cd contracts; forge test --match-path test/EmtunSimulationSmoke.t.sol -vvv` |
| 28. Authorization status view | `1f8d10c` | Shipped | Read-only agent authorization snapshot across registration, attestation, root, and SAP proof state | SDK clients can ask one contract whether an agent is registered, attested, and authorized for an action | `cd contracts; forge test --match-path test/EmtunAuthorizationStatusView.t.sol -vvv` |
| 29. Lifecycle status flags | `1f8d10c` | Shipped | Boolean lifecycle flags added beside enum status fields | Clients can consume lifecycle state without duplicating enum decoding logic | `cd contracts; forge test --match-path test/TaskLifecycleView.t.sol -vvv` |
| 30. SDK read interfaces | `1f8d10c` | Shipped | Interfaces for authorization status and lifecycle read surfaces | External integrations can target stable read contracts without write authority | `cd contracts; forge test --match-path test/SdkReadInterfaces.t.sol -vvv` |
| 31. Lifecycle view stateful fuzz | `1f8d10c` | Shipped | Fuzz check that lifecycle snapshots mirror underlying task contracts | Read-only observability cannot drift from canonical market, escrow, result, and acceptance state | `cd contracts; forge test --match-path test/TaskLifecycleViewStatefulFuzz.t.sol -vvv` |
| 32. Authorization view deployment | `1f8d10c` | Shipped | Deployment script and wiring test include authorization status view | Local simulation deployment includes the SDK-facing authorization read surface | `cd contracts; forge test --match-path test/DeploySimulation.t.sol -vvv` |
| 33. SAP fixture audit | `e62ccc9` | Shipped | TypeScript audit for exported verifier-call fixture schema, field order, public inputs, proof bytes, and proof hash | Generated proof fixtures become reproducible review artifacts rather than trusted blobs | `cd scripts; npm run sap-fixture-audit` |
| 34. Validation includes fixture audit | `e62ccc9` | Shipped | `npm run validate` now runs the SAP fixture audit after verifier-call export | One command validates Poseidon2 alignment, Merkle inclusion, negative rejection, export, and fixture metadata | `cd scripts; npm run validate` |
| 35. Read surface gas | `e62ccc9` | Shipped | Gas baselines for authorization status and lifecycle read views | Reviewers can separate expensive proof-backed reads from lightweight lifecycle observability | `cd contracts; forge test --match-path test/ReadSurfaceGas.t.sol -vvv` |
| 36. Primitive boundary smoke | `e62ccc9` | Shipped | Compact smoke test logging `policy_root`, `action_hash`, authorization status, and execution non-goal | Public screenshots can show the SAP boundary without mixing in settlement semantics | `cd contracts; forge test --match-path test/PrimitiveBoundarySmoke.t.sol -vvv` |
| 37. Reproducibility screenshot path | `e62ccc9` | Shipped | Notes and README commands for fixture audit plus primitive boundary smoke | Contributors get a direct path to reproduce the core public claim and fixture metadata | `cd scripts; npm run sap-fixture-audit` |

## Open Build Queue

- Settlement is limited to requester acceptance of a committed result hash; execution correctness remains out of scope.
- Execution verification remains out of scope for V1 unless a separate primitive is introduced.
- Production leaf schema remains unresolved; changing it after deployment invalidates all policy commitments.
- Recursive aggregation remains a later verifier economics milestone, not a simulation blocker.
