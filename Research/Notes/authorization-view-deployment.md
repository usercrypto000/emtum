# Authorization View Deployment
*May 2026*

The deployment simulation now includes `EmtunAuthorizationStatusView`, and the wiring test asserts that it points to the intended registry, attestation boundary, and authorization reader. This keeps the local deployment aligned with the SDK-facing read surface.
